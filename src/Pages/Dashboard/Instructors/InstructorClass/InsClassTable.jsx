import React from 'react';

const InsClassTable = ({ classs }) => {

    const { _id, instructorName, availableSeats, image, price, name } = classs || {};

    return (
        <div>
            
        </div>
    );
};

export default InsClassTable;